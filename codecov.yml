coverage:
  status:
    patch: false
    project:
      default:
        target: '90'
        threshold: '0.1'
        branches: null
        only_pulls: false

  ignore:
    - spec/.*
    - vendor/.*
